# CPU-IPC
Concise CPU Incremental Potential Contact Simulation

## Dependencies
Windows:
```bash
vcpkg install eigen3 freeglut glew tbb openblas suitesparse metis
```

Ubuntu:
```bash
sudo apt install libeigen3-dev freeglut3-dev libglew-dev libtbb-dev libopenblas-dev libsuitesparse-dev metis
```